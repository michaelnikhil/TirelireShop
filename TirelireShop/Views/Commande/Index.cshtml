@model IEnumerable<TirelireShop.Commande>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdclientNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdstatutCommandeNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdclientNavigation.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdstatutCommandeNavigation.StatutCommande1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idcommande">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idcommande">Details</a> |
            </td>
        </tr>
}
    </tbody>
</table>
