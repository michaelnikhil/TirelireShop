@model IEnumerable<TirelireShop.StatutCommande>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StatutCommande1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StatutCommande1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdstatutCommande">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdstatutCommande">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdstatutCommande">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
